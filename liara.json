{
  "app": "adoring-cannon-h3bzlznb2",
  "port": 8000,
  "platform": "python",
  "build": {
    "location": "iran"
  },
  "command": "python3 -m streamlit run app.py --server.port 8000 --server.address 0.0.0.0 --server.headless true",
  "healthCheck": {
    "command": "curl --fail http://localhost:8000/_stcore/health || exit 1",
    "interval": 30,
    "timeout": 10,
    "retries": 3,
    "startPeriod": 20
  }
}
